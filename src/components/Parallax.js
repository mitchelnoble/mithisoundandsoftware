import React from "react";

const Parallax = () => {
  return (
    <div>
      {/* The page should have a few geometric shapes in one layer of the parallax fitting into a slot or hole perfectly in another layer
      to symbolize the ideal aspect of the Mithi brand. Each card, page graphic, soundcloud playlist, etc. can have an animation that slides
      into a slot. It uses a sort of triple shadow effect that mimics the color and then slides into place. A 1-2-3 count (tic, tic, tic, *click*) and then moves
      into position. */}
    </div>
  );
};

export default Parallax;
